

<div class="container-lg mt-5 mx-auto" style="width: 750px;">
 <style>
  .field{margin-bottom: 10px}
  dt{
   width: 30%;
   font-weight: normal;
   }
  dd{width:70%;}
 </style>
 <h4 class="mb-5"><span class="bg-light">新規会員登録</span></h4>

 <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
   <%= render "public/shared/error_messages", resource: resource %>
     <div class="field d-flex">
      <dt><%= f.label :"メールアドレス" %></dt>
      <dd><%= f.email_field :email, autofocus: true %></dd>
     </div>
     <div class="field d-flex">
      <dt><%= f.label :"ニックネーム (20文字まで)" %></dt>
      <dd><%= f.text_field :nickname, autofocus: true %></dd>
     </div>
     <div class="field d-flex">
      <dt><%= f.label :"自己紹介(任意 200文字まで)" %></dt>
      <dd><%= f.text_field :introduction, autofocus: true %></dd>
     </div>
     <div class="field d-flex">
      <dt><%= f.label :"パスワード（6文字以上）" %></dt>
      <dd><%= f.password_field :password %></dd>
    </div>
    <div class="field d-flex">
     <dt><%= f.label :"パスワード（確認用）" %></dt>
     <dd><%= f.password_field :password_confirmation %></dd>
    </div>
    <div class="actions d-flex justify-content-center">
     <%= f.submit "新規登録",class:"btn btn-success pl-5 pr-5" %>
    </div>
   <% end %>

 <h4 class="mt-5"><span class="bg-light">既に登録済みの方</span></h4>
 <%= render "public/shared/links" %><p>からログインしてください。</p>
</div>